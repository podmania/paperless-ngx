name: Build & Publish

permissions:
  contents: write
  packages: write

on:
  workflow_dispatch:
    inputs:
      force_rebuild:
        description: 'Force rebuild current version'
        required: false
        default: false
        type: boolean

jobs:
  build:
    uses: podmania/.github/.github/workflows/build-publish.yml@main
    with:
      package: paperless-ngx
      version-output: paperless-ngxVersion
      images: '[{"name":"paperless-ngx","package":"paperless-ngx-image"}]'
      force_rebuild: ${{ github.event.inputs.force_rebuild == 'true' }}
